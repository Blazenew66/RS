# 数据要求说明

## 1. "有效数据点不足" 的含义

### 什么是"有效数据点不足"？

系统在计算 RS（相对强度）排名时，需要**至少 262 个有效交易日**的历史数据。

### 为什么需要 262 个交易日？

1. **RS 计算周期要求**：
   - 系统需要计算 12 个月的 RS（252 个交易日）
   - 加上 10 个缓冲交易日，总共需要 **262 个交易日**

2. **数据对齐要求**：
   - 股票数据必须与市场基准（SPY）的日期对齐
   - 对齐后，两个序列都必须有至少 252 个共同的交易日

### 什么情况下会出现"有效数据点不足"？

1. **新股**：刚上市不久的股票，历史数据不足 1 年
2. **停牌股票**：长期停牌导致数据缺失
3. **数据不完整**：从 Stooq 下载的数据文件不完整
4. **退市股票**：已经退市，数据被移除
5. **数据质量问题**：文件中包含大量无效数据（NaN、0值等）

### 系统如何处理？

- 系统会**自动跳过**这些股票，不会影响其他股票的计算
- 这些警告信息只是提醒，不是错误
- 可以安全地忽略这些警告

## 2. 数据文件要求

### 文件格式
- 文件名：`股票代码.US.txt` 或 `股票代码.US.csv`（大小写不敏感）
- 例如：`AAPL.US.txt`、`MSFT.us.txt`、`SPY.US.csv`

### 必需列
- `<DATE>`：日期（格式：YYYYMMDD）
- `<CLOSE>`：收盘价（用于计算 RS）

### 可选列
- `<OPEN>`：开盘价
- `<HIGH>`：最高价
- `<LOW>`：最低价
- `<VOL>`：成交量

### 数据质量要求
- 文件不能为空（0 字节）
- 至少需要 262 个有效交易日的数据
- 成交量不能全为 0（系统会自动过滤成交量为 0 的行）

## 3. 已删除的空文件

刚才已删除 **455 个空文件**，这些文件：
- 大小为 0 字节
- 无法被系统读取
- 会导致系统尝试使用 yfinance 作为备用（如果启用）

删除这些空文件后：
- 系统运行更快（不需要尝试读取空文件）
- 减少不必要的警告信息
- 减少对 yfinance 的依赖（如果禁用增量更新）

## 4. 建议

1. **定期清理**：可以定期运行 `clean_empty_files.py` 清理空文件
2. **检查数据完整性**：如果某个重要股票一直显示"有效数据点不足"，可以：
   - 检查该股票的数据文件是否完整
   - 重新从 Stooq 下载该股票的数据
3. **忽略警告**：对于新股或停牌股票，这些警告是正常的，可以忽略

